# Next.js RTK Query demo

Simple demo of working with rtk-query.

Built using next.js, chakra-ui, rtk-query, json-server


## Test server

### Install json server 

`npm install -g json-server
`

### Serve

` json-server --watch server/db.json --port 5000`

### Run
`npm run dev`
